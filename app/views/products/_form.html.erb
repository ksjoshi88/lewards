<%= form_with(model: product, local: true) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :interest_rate %>
    <%= form.number_field :interest_rate %>
  </div>

  <div class="field">
    <%= form.label :reward_points_factor %>
    <%= form.text_field :reward_points_factor %>
  </div>

  <div class="field">
    <%= form.label :rewards_validity_duration %>
    <%= form.number_field :rewards_validity_duration %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
