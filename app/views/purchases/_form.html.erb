<%= form_with(model: purchase, local: true) do |form| %>
  <% if purchase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(purchase.errors.count, "error") %> prohibited this purchase from being saved:</h2>

      <ul>
      <% purchase.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :loan_type %>
    <%= ":" + purchase.product.name %>
  </div>

  <div class="field">
    <%= form.label :interest_rate %>
    <%= ":" + purchase.product.interest_rate.to_s %>
  </div>

  <div class="field">
    <%= form.label :loan_amount %>
    <%= form.text_field :amount %>
  </div>

   <%= form.hidden_field :product_id, value: purchase.product.id %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
